import { View, Text, StyleSheet } from "react-native";
function Focus(navigation) {
  return <Text style={styles.conatiner}>Focus</Text>;
}
export default Focus;
const styles = StyleSheet.create({
  conatiner: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
