import { View, Text, StyleSheet } from "react-native";
function Calender({ navigation }) {
  return <Text style={styles.conatiner}>Calender</Text>;
}
export default Calender;
const styles = StyleSheet.create({
  conatiner: {
    flex: 1,
    alignItems: "center",
    justifyContent: "center",
  },
});
